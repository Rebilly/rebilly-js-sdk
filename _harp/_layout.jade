doctype
html
    head
        script(src="#{ relativeAssets }assets/jquery-3.2.1.min.js" defer)
        script(src="#{ relativeAssets }assets/semantic.min.js" defer)
        script(src="#{ relativeAssets }assets/main.js" defer)
        script(src="https://embed.runkit.com")
        link(rel="stylesheet" href="#{ relativeAssets }assets/main.css" type="text/css")
        title #{ title }
    body.pushable
        != partial("_menu")
        div.pusher
            div.ui.container
                != yield
                footer.ui.basic.segment
                    .ui.small.header
                        p Copyright (c) 2017, Rebilly, Inc.
